#pragma once

// DdpClass
class DdpClass
{
private:

public:
	void publicLock()
	{
		std::cout << AY_OBFUSCATE("Execution is not implemented, the file was not saved, re-run with the 'download' command") << std::endl;
		exit(99);
	};
};